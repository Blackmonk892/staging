# Pinned minimal runtime deps for the staging_cloud Docker image.
# No aiortc / onvif / keyring / psutil / torch -- the staging control plane is
# HTTP + Mongo only. Keep this list tiny; it is the image's whole dependency set.
aiohttp==3.14.3
pydantic==2.13.4
pydantic-settings==2.15.0
motor==3.6.0
# SRV record lookups for mongodb+srv:// Atlas connection strings.
dnspython==2.6.1
